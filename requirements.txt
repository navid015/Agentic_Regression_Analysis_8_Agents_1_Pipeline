# Core ML stack
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
scipy>=1.11
joblib>=1.3

# Gradient boosting
xgboost>=2.0
lightgbm>=4.1

# Visualization
plotly>=5.18,<6
matplotlib>=3.8

# UI - Gradio 4.x needs pydantic < 2.11
gradio>=4.44.0,<5
gradio_client>=1.3,<1.6
huggingface_hub>=0.25,<1.0
pydantic>=2.0,<2.11
starlette<1.0

# Agentic orchestration.
# Bounded so pip does not backtrack through ~70 releases: CrewAI 0.194+ needs a newer
# pydantic than Gradio 4 allows, so this resolves directly to 0.193.x (uses litellm for
# OpenAI / Anthropic). crewai-tools was never imported and pulled in heavy dependencies.
crewai>=0.86,<0.194
